import { StyleSheet } from 'react-native';
// import { STYLE } from './constants';

export default StyleSheet.create({
  container: {
    padding: 30,
    marginTop: 100,
    marginBottom: 20,
  },
  textInput: {
    marginBottom: 20,
    borderWidth: 1,
  },
  button: {
    marginTop: 10,
  },
});
